#Run example, including placeholder substitution:
  #cat payment-service-service.yaml | sed 's/__ENVIRONMENT'"/ci/g"  |kubectl apply -f -
#TODO: support variables: $SERVER_ROLE, PORT
#TODO support labels?
#TODO: replace load balancer with Ingress

apiVersion: v1
kind: Service
metadata:
  name: payment-web
  namespace: __ENVIRONMENT
  labels:
    app: payment-web
    name: payment-web
spec:
  type: NodePort
  ports:
   - port: 80
     #SSM: APP_PORT
     targetPort: 5000
  selector:
   app: payment-web
